{
  "name": "spritefont-loader",
  "version": "0.0.2",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "prepublish": "npm run build",
    "test": "npm run build && mocha",
    "prettier": "./node_modules/.bin/prettier --write --single-quote 'src/**/*.js'"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:andormade/browser-spritefont.git"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "canvas-prebuilt": "^1.6.5-prerelease.1",
    "jsdom": "^11.6.2",
    "jsdom-global": "^3.0.2",
    "mocha": "^5.0.1",
    "prettier": "^1.10.2"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0"
  }
}
